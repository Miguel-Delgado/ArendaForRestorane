
## 🧠 ОБЩАЯ ИДЕЯ

Программа имитирует **систему управления рестораном**.
Она объединяет несколько модулей:

* управление **меню блюд** (CRUD — добавить, удалить, изменить, просмотреть),
* управление **столами** (добавление, удаление, просмотр),
* управление **бронированиями**,
* интерфейс **гостя** для заказа еды и брони стола.

---

## 📂 СТРУКТУРА КЛАССОВ

### **1. Dish**

> Модель блюда в меню.

```csharp
class Dish
{
    public string Category { get; set; }   // Категория (Закуски, Основные блюда и т.д.)
    public string Name { get; set; }       // Название
    public decimal Price { get; set; }     // Цена
}
```

Используется для формирования списка `menu`, который содержит все блюда ресторана.

---

### **2. Table**

> Модель стола в ресторане.

```csharp
class Table
{
    public int TableId { get; set; }               // Уникальный номер стола
    public string Location { get; set; }           // Расположение (Зал, У окна и т.д.)
    public int Seats { get; set; }                 // Количество мест
    public Dictionary<string, string[]> Schedule;  // Расписание брони (дата → массив времён)
}
```

Используется в списке `tables`, где хранятся все столы.

---

### **3. Booking**

> Модель бронирования.

```csharp
class Booking
{
    public int TableId { get; set; }       // ID стола
    public string ClientName { get; set; } // Имя клиента
    public int GuestId { get; set; }       // Уникальный ID гостя
    public string BookingTime { get; set; }// Время брони
    public string BookingDate { get; set; }// Дата брони
}
```

Используется в словаре `bookings` — ключом является `GuestId`, значением объект `Booking`.

---

## 🧩 ГЛОБАЛЬНЫЕ КОЛЛЕКЦИИ

```csharp
static List<Table> tables = new List<Table>();
static Dictionary<int, Booking> bookings = new Dictionary<int, Booking>();
static List<Dish> menu = new List<Dish>();
```

* `tables` — все столы ресторана
* `bookings` — активные бронирования
* `menu` — все блюда
* `guestIdCounter` — счётчик гостей (уникальные ID)
* `tableIdCounter` — счётчик для добавления новых столов

---

## 🔁 ЛОГИКА РАБОТЫ ПРОГРАММЫ

### **1. Main()**

> Точка входа.
> Инициализирует столы и меню, затем запускает `MainMenu()`.

---

### **2. MainMenu()**

> Главное меню выбора типа пользователя.

```plaintext
Введите тип пользователя:
1. администратор
2. гость
```

* если введено “администратор” → переход в `AdminLogin()`
* если “гость” → `GuestMenu()`
* иначе повтор запроса

---

## 👑 РЕЖИМ АДМИНИСТРАТОРА

### **AdminLogin()**

Проверяет пароль (`admin123` по умолчанию).
Если верно → `AdminMainMenu()`.
Если нет → возвращает в главное меню.

---

### **AdminMainMenu()**

Основная панель администратора:

1. Меню
2. Столы
3. Брони
4. Выход

---

#### **1️⃣ MenuManagementAdmin() — управление меню**

Возможности:

* Просмотреть меню (`ShowMenuAdmin`)
* Добавить блюдо (`AddDish`)
* Удалить блюдо (`RemoveDish`)
* Редактировать блюдо (`EditDish`)

---

#### **2️⃣ TablesManagement() — управление столами**

Функции:

* Добавить стол (`AddTable`)
* Удалить стол (`DeleteTable`)
* Просмотреть информацию (`ShowTablesInfo`)

Таблицы имеют ID, количество мест и расположение.

---

#### **3️⃣ BookingManagement() — управление бронированиями**

Функции:

* Добавить бронь (`AddBookingAdmin`)
* Изменить (`ModifyBookingAdmin`)
* Удалить (`DeleteBookingAdmin`)
* Просмотреть все брони (`ShowAllBookingsAdmin`)

Каждая бронь проверяет доступность стола по дате и времени.

---

## 🙋 РЕЖИМ ГОСТЯ

### **GuestMenu()**

Меню для обычного клиента:

1. Сделать заказ
2. Забронировать стол
3. Посмотреть меню
4. Посмотреть карту ресторана
5. Выход

---

#### **1️⃣ MakeOrder() — оформление заказа**

Показывает меню, позволяет добавлять блюда, считает итоговую сумму, подтверждает заказ.

---

#### **2️⃣ ReserveTableGuest() — бронирование**

Выводит список столов, просит:

* имя клиента
* дату
* время

Проверяет, свободен ли стол, и сохраняет бронь.

---

#### **3️⃣ ShowMenuGuest()**

Выводит текущее меню ресторана (без админских функций).

---

#### **4️⃣ ShowRestaurantMapGuest()**

Показывает расположение всех столов с их ID и местами.

---

## ⚙️ ВСПОМОГАТЕЛЬНЫЕ МЕТОДЫ

* `GetValidInt()` — безопасный ввод целых чисел.
* `GetValidDecimal()` — безопасный ввод цены.
* `ShowAllTables()` — список всех столов.
* `ShowTableDetails()` — подробная инфа по выбранному столу.

---

## 🌳 ОСНОВНЫЕ ВЕТКИ ПРОГРАММЫ (по структуре)

```
RestaurantApp
├── Main()
│   ├── InitializeTables()
│   ├── InitializeMenu()
│   └── MainMenu()
│
├── [MainMenu()]
│   ├── AdminLogin()
│   │   └── AdminMainMenu()
│   │       ├── MenuManagementAdmin()
│   │       ├── TablesManagement()
│   │       └── BookingManagement()
│   │
│   └── GuestMenu()
│       ├── MakeOrder()
│       ├── ReserveTableGuest()
│       ├── ShowMenuGuest()
│       └── ShowRestaurantMapGuest()
│
└── Helper Methods (GetValidInt, GetValidDecimal, ShowAllTables...)
```

---

## 📘 РЕКОМЕНДУЕМОЕ СОДЕРЖАНИЕ `README.md`

````markdown
# 🍽️ RestaurantApp

Консольное приложение на C#, моделирующее работу ресторана.  
Позволяет управлять меню, столами и бронированиями как администратору,  
а также делать заказы и бронировать столы как гостю.

---

## 🚀 Функционал

### 👑 Для администратора:
- Управление меню (добавление, удаление, редактирование блюд)
- Управление столами (создание, удаление, просмотр)
- Управление бронированиями (добавление, изменение, удаление)
- Просмотр всех активных броней

### 🙋 Для гостя:
- Просмотр меню ресторана
- Бронирование стола
- Создание заказа
- Просмотр карты ресторана

---

## 🧱 Структура проекта

- **Dish.cs** — модель блюда  
- **Table.cs** — модель стола  
- **Booking.cs** — модель брони  
- **RestaurantApp.cs** — основная логика программы  

---

## 🔐 Данные по умолчанию

- Пароль администратора: `admin123`
- Изначально 16 столов (по 4 места)
- Пример меню добавляется при запуске

---

## ⚙️ Запуск

```bash
dotnet run
````

или

```bash
RestaurantApp.exe
```

---

## 📄 Автор

Разработано для учебных целей.
Система демонстрирует базовые принципы **ООП, коллекций, CRUD-операций и консольного интерфейса** на языке **C#**.

```

---

Хочешь, я сделаю тебе красивый `README.md` сразу с форматированием, эмодзи, и вставкой диаграммы дерева функций в виде ASCII-блока, чтобы можно было вставить прямо в GitHub?
```
